- Ajouter un message de cryptage (en mode connecte) dans le protocol : DWORD CryptType, ... DATAs (de la taille du packet - entete)
  Une fonction a qui on passe le CryptType et les data (cle privee/publique par ex) (et la socket, si il doit communiquer avec l autre bout pour un echange)
  Une fonction dans le thread de message (juste avant analyse_packet) a qui on passe le packet pour decryptage
  Mettre ces 2 fonctions dans un fichier crypt.c

- Dans le common.c, qd un message est ignore, ajouter le nom de la machine dans une liste des
 machines dont le message a ete refuse, et eventuellement logger dans un log different.
 Au bout d un certain nombre de fois ou la machine est dans cette liste, bannir la machine

 - Ajouter une option dans les fonctions de transfert : Taille des blocs de checksum (0=fichier complet, x=chksum envoye tous les x octets pour check)

 - TRANSFORMER LES CONSTANTES DES PORTS en variables (enfin, garder les 2, par defaut, on assigne les constantes au var) et mettre les var en globales

 - optimiser les messages (ne pas faire de copie des buffers, mais envoyer plusieurs paquets)
     >> [11:46:04] <kiki[Artabel]>  writev
     >> [11:46:09] <kiki[Artabel]>  WSPSend
     >> [11:46:16] <kiki[Artabel]>  pour envoyer des buffers a concatener


dnl Check endian
AC_C_BIGENDIAN
if test "${ac_cv_c_bigendian}" = "yes"; then
  echo "WARNING!"
  echo "SkySOUND hasn't been tried yet with BIGENDIAN processors! If you"
  echo "experiment problems, please contact the development team! We want"
  echo "to have feedback about this problem!"
  echo "Note that you may be unable to use SkySOUND."
  echo ""
fi

